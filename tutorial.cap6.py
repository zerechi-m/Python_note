# 6.1 ) モジュールについて

  # 処理をモジュールとしてまとめたい処理を、コードエディタで .pyの拡張子で保存する
  # インタープリタ（ターミナル)にてpythonを開き、import xx.py を入力する
  # >>> ファイル名.関数名(引数) で モジュールを呼び出し
  # モジュールを頻繁に使用する際には x = ファイル名.関数名とローカル変数に代入することで x(引数)でモジュールを呼び出し
  